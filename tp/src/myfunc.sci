// Quelque fonction pour tester les codes

// Calcul x au carré (x^2)
function [res] = mysquare(x)
	 res = x * x;
endfunction

// Calcul x au cube (x^3)
function [res] = mycube(x)
	 res = x * x * x;
endfunction
